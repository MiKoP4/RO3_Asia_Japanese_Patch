# Recovery停止後の翻訳漏れ調査（2026-09-23）

## 2026-09-24: 露店の商品名と生活スキル経験通知（2.7.32）

露店の実画面で`紅辣椒`と`紅色藥草`が残っていた。公式の署名済みローカライズを読み取り専用で照合すると、それぞれアイテムID `12390001307`（`Red Chili Pepper`→「赤唐辛子」）と`12390001302`（`Red Herb`→「赤ハーブ」）だった。個別の別名を増やすだけではほかの商品が残るため、同じ原本からアイテム名ID `123900`の1,815行を抽出し、正規英語IDと既存日本語訳に突き合わせた。翻訳済み1,812行から簡体字・繁体字の一意な別名3,335件を生成して優先辞書と描画用ID別名に加えた。日本語訳を意図的に英語のまま保つ`Base EXP`・`Job EXP`・`HIT`は変えず、同じ中国語名が異なるアイテムを指す3件は誤訳を避けて除外した。署名済み原本は変更していない。

生活スキルの獲得通知に残った`Gardener`は、英語IDの訳と複合表示の双方に原因があった。職業名を「採掘師・園芸師・調理師」に統一し、正規ID9件を上書き対象に加えた。経験値通知は職業名だけを既存の訳へ置換し、数値と装飾を保持する。スクリーンショットの通知・露店商品と未知名を再現する検査を追加した。辞書生成の`--check`、表示79/79、履歴90,419/90,419、Recovery6/6、DLLビルドが成功。実機適用・目視確認はゲームの通常終了後に行う。

## 2026-09-24: 依頼トラッカーの英語混在（2.7.32）

実画面の依頼欄で`[Commission] Defeat Monsters (2/10)`と`Geffen Outskirtsで… 0/20`を確認した。署名済みLocalizationには`[Commission]`、`Defeat Monsters (@{1}/@{2})`、`Geffen Outskirts`の正規訳が存在する。表示時に見出しの題名へ進捗数を埋め込むと、従来の見出し処理は題名の完全一致しか調べない。また、地名を日本語の目的文に差し込むと、文全体は辞書に存在しない。

見出し処理に既存の動的テンプレート翻訳を通し、装飾記号を除いた依頼種別も正規IDから登録した。目的文は既知の地名が先頭にあり、末尾が進捗数の形に限って地名を訳す。未知の地名や自由文を変えない再現例も追加した。表示71/71、履歴84,167/84,167、DLLビルドが成功。利用者の依頼画面は消えており、翌日に再確認する予定。ゲームは起動中のため配備と実画面確認は未実施。

## 2026-09-24: 孵化履歴のガチャ枠名（2.7.31）

実画面の孵化履歴で、見出しと説明は日本語だがタブと「ガチャ種類」列に中国語の`常驻卡池`が残り、期間限定側も中国語だった。署名済みLocalizationのID `40209`/`40210`は英語の`Permanent Pool`/`Limited-Time Pool`に対応する。従来の訳「常設召喚」と「期間限定プール」は同じ`Pool`を別の用語にしていたため、前者を「常設プール」へ統一した。

画面で確認できた`常驻卡池`と、文字形の異なる`常駐卡池`、期間限定側の候補`限时卡池`/`限時卡池`/`限定卡池`を別名として追加した。タブと履歴行のどちらも同じ訳になる。署名済みの公式Recoveryファイルは変更しない。辞書生成の`--check`、履歴84,164/84,164、表示66/66、DLLビルドが成功。

利用者の通常終了後に2.7.31を適用し、実プロセスで`Loading [RO3 Localization Table Patcher 2.7.31]`と`[Regression] passed=583/583`を確認した。配備検証はペイロード6件一致、生成キャッシュ追記のみ、保護対象6件不変、失敗0件。バックアップは`../_TranslationWorkspace/deploy-backups/display-2.7.31-ac0779491c104ad7b34649d1452d0d99`。再起動後の実画面を利用者のクリップボード画像で確認し、左タブの「常設プール／期間限定プール」と履歴の「ガチャ種類」列の「常設プール」が日本語になっている。

## 2026-09-24: 自動解体通知のアイテム名（2.7.30）

実画面の「Earrings X 1を自動解体し、Minted Coin X 50を…」と実機ログの原文を照合した。原文は`Automatically dismantled <color=#6890D2>Earrings</color> X 1 and obtained <color=#6890D2>Minted Coin</color> X 50.`で、テンプレート文のみ翻訳され、`${1}`と`${2}`に差し込まれたアイテム名が残っていた。両名の正規訳はアイテムID `12390000507`の「イヤリング」と`12390000243`の「鋳造されたコイン」。同じログではRod、Ring、Sandals、Cotton Shirtでも再現した。

自動解体通知の英語／日本語の完全な文型を識別し、色タグと数量を保持したまま、両方のアイテム欄だけ既存のアイテム辞書で翻訳する。未知の名前は維持する。元の英語文、翻訳済み文、既知／未知名混在の再現例を追加した。

横断調査では、この実機セッションの`DisplayTrace`134件をアイテム・魔物名の正規辞書と照合し、日本語文中に既知の英語名が残った12記録はいずれも自動解体通知だった。ほかの英語片はプレイヤー名・自由文、または正規訳も英語の`Job EXP`だった。隣接する翻訳テンプレートも調べ、同じ差し込み方をする手動解体（`46016`）、合成成功（`46022`）、数量付き獲得（`1049`）を再現例で確認して、既知のアイテム名を訳すようにした。既に単独で表示される獲得アイテム名は実機ログで日本語化されており、その処理は維持する。表示66/66、履歴84,149/84,149、DLLビルドが成功。利用者の通常終了後に2.7.30を適用し、実プロセスのロードと`[Regression] passed=578/578`を確認した。配備検証はペイロード6件一致と辞書キャッシュ追記1,301バイト、保護対象6件不変、失敗0件。バックアップは`../_TranslationWorkspace/deploy-backups/display-2.7.30-98a5863f9dfe4fcba61940022f37d07b`。自然発生した自動解体通知の再観測は待機中。

## 2026-09-24: 通常討伐通知の魔物名（2.7.29）

2.7.28を実機で起動し、`[Regression] passed=570/570`を確認した後、チャットに「Moonlight Flowerは…に倒されました。」が残る実画面が届いた。これはレア報酬文（ID `10320000305`）とは別の通常討伐通知（ID `10320000304`、`${1} was defeated by ${2}.`）で、`${1}`が魔物名、`${2}`が討伐者名。`Moonlight Flower`の正規訳「月夜花」は魔物名ID `10470000035`に既に存在する。

英語／日本語の通常討伐通知でも文型全体を確認し、先頭の魔物名だけを正規の魔物名辞書で訳す。討伐者名、未知の魔物名、色タグは保持する。実画面と同じ混在文、英語文、色付き文、討伐者名の負例を表示・実機検査に追加した。表示57/57、履歴84,144/84,144、DLLビルドが成功。利用者の通常終了後に2.7.29を適用し、実プロセスのロードと`[Regression] passed=573/573`を確認した。配備検証はペイロード6件一致と辞書キャッシュへの追記5,115バイト、保護対象6件不変、失敗0件。バックアップは`../_TranslationWorkspace/deploy-backups/display-2.7.29-1fabf3096d0d4803a30b064cd2b300e8`。自然発生した通常討伐通知の再観測は待機中。

## 2026-09-24: レア報酬アナウンスの魔物名（2.7.28）

実画面で「プレイヤー【…】が【Eddga】を撃破し、レア報酬を獲得しました！」と表示された。文面の翻訳テンプレート（Localization ID `10320000305`）は変数 `${1}` に魔物名を差し込むため、文面だけが日本語化され、差し込み後の魔物名が英語のまま残っていた。`Eddga` の正規訳「エドガ」は魔物名ID `10470000368` に既に存在する。

描画時にレア報酬アナウンスの英語／日本語の完全な文型を識別し、魔物名の欄だけ既存の魔物名辞書で変換する。プレイヤー名、自由文、未知の魔物名、色タグは維持する。`Eddga` に限定せず、辞書にある他の魔物名にも同じ処理を適用する。魔物名IDは1,294件、英語名は1,273種類で、同じ英語名に異なる訳が付いた衝突は0件。再現例と別魔物名、色付き文面、プレイヤー名の負例を追加した。表示52/52、履歴84,141/84,141、DLLビルドが成功。実機では2.7.28のロードと570/570件の検査成功を確認した。レア報酬の自然発生イベントはまだ再観測できていない。

## 2026-09-24: アコライトのヒール前提条件（2.7.27）

実機ログの`Prerequisite Skill <color=#cc762a>治愈术 Lv.5 </color>`が見出しだけ「前提スキル」になり、技能名が中国語のまま残った。署名済みLocalizationのID `10110200003`では英語`Heal`、簡体字`治愈术`、繁体字`治癒術`に対応し、既存の正規訳は「ヒール」。技能名の完全一致別名を両中国語に追加して、スキルツリーと前提条件の表示を統一した。別技能`Divine Light`のID `10110200819`は中国語に注記が付く別の原文なので、この完全一致別名の対象外。再現例と繁体字例を履歴検査へ追加し、表示47/47、履歴84,138/84,138、辞書`--check`、DLLビルドが成功。利用者の通常終了後、2.7.27を適用して公式ランチャーから起動した。実プロセスの`Loading [RO3 Localization Table Patcher 2.7.27]`、`[Regression] passed=567/567`を確認。実画面ではスキル名が「ヒール」、別スキルの前提条件が「ヒール Lv.5」と表示された。今回の画面で原文は`Heal Lv.5`だったが、先に観測した中国語の形も履歴検査で確認済み。配備検証はペイロード6件一致、保護対象6件不変、失敗0件。バックアップは`../_TranslationWorkspace/deploy-backups/display-2.7.27-fdca25e28d3040b6b52c1ca10c7abbbf`。

## 2026-09-24: 2.7.25実機再調査とメール容量（2.7.26）

実機の共鳴プレビューにて「楽句」見出し・説明・リンクが一致し、「ローハム」の左スキルと右バフの見出しも同じ語になったことを目視確認した。左右の効果文の違いは公式原文の`skill/11431301`と`buff/11431301`に対応する。ゲームログの表示変換記録でも両リンクの訳とスキル／バフ見出しを確認した。

2.7.25の実機ログを再検査した結果、メール容量の`邮件存储上限：${1}/${2}`が未訳のまま描画されたことを発見。署名済み原文ではID 20021/20029の英語`Mail Storage Limit`に対応する中国語だった。中国語の簡体／繁体、装飾あり／なしの別名を同じ「メール保存上限」へ結び、展開済みの数値も処理した。ローカル検査は表示47/47、履歴84,128/84,128、辞書再生成の`--check`に成功した。利用者の通常終了後に2.7.26を適用し、公式ランチャーから起動した。実ゲームのログで`Loading [RO3 Localization Table Patcher 2.7.26]`、`[Regression] passed=561/561`、メール容量の`邮件存储上限：${1}/${2} => メール保存上限：${1}/${2}`を確認し、画面下部でも日本語の「メール保存上限」を確認した。配備検証はペイロード6件一致、保護対象6件不変、メール関連の実表示6/6翻訳、残存原文0件、失敗0件。バックアップは`../_TranslationWorkspace/deploy-backups/display-2.7.26-ff5b4644e3434cc8a22f0bc289408e91`。

## 2026-09-24: 追加画像とスキル用語再監査（2.7.25）

追加画像の「楽句」は、同じ原語が説明文で「楽節」、リンクで「フレーズ」になっていた。公式Localizationの中国語もいずれも「乐句／樂句」であり、見出し・説明・リンクを「楽句」に統一した。「低鸣／低鳴」はスキル見出し「ささやき」、バフ見出し「低音ドローン」、リンク「ローハム」に割れていたため「ローハム」に統一した。バトルチャント・ハーモニーの同じ「協和音」、インテンス・テレキネシス、ウォーグ関連なども統一した。

署名済みLocalizationのスキル・バフ見出し3,463件と説明5,610件をIDで照合した。英語名が完全一致する見出しと説明内リンクの不一致24語を修正し、中国語見出しが句読点差のみで同じ14組を検討した。結果として「開炮！」の`Fire!`と「開炮」の`Fire Cannon`だけは別の原語・スキルとして残した。説明内で同じ中国語を使う異なる英語名については、同一効果と確認できた楽句、ローハム、ボイスパート、ブックページ、ウォーグハントなどを統一した。変装とカモフラージュ、減速と鈍足、ノックバックと打ち上げ、別人名や別スキルの区別は維持した。辞書生成時に確定したスキル名・装飾付きリンクを検査するようにした。

画像2・3のローハムは、同じ数値IDでも`skill/11431301`と`buff/11431301`という異なる種類のリンクである。前者は命中時の追加聖属性ダメージ（Localization ID `10110300830`）、後者は3秒間・最大20スタックの状態効果説明（`10220300769`）を表示していた。両者の内容差は原文データにも存在する。数値IDや効果文を差し替える修正は行っていない。

2.7.25の辞書とDLLを作成し、表示47/47、履歴84,113/84,113、Recovery6/6、辞書再生成の`--check`を通した。利用者の通常終了後に7ファイルをバックアップしてゲームへ適用した。実ゲームのログで`Loading [RO3 Localization Table Patcher 2.7.25]`と`[Regression] passed=554/554`を確認。配備検証ではペイロード6件一致、生成キャッシュは追記のみ、保護対象6件不変、失敗0件。ログイン後のフィールド表示を目視確認した。楽句・ローハムの該当画面の再確認は進行中。配備バックアップは`../_TranslationWorkspace/deploy-backups/display-2.7.25-0f83ba3335b34c429074f93153c44611`。

## 2026-09-24: スクリーンショット9件と訳語統一

Git履歴の`b64e652`、`31eeac8`、`2588e55`、`123975c`と作業ツリーの未コミット差分を照合した。署名済みRecovery Luaの直接変更は行わず、表示翻訳・ID上書き・辞書へ修正を追加した。郵件、篩選、排行、活動尚未開啟、スキルツリーの審判・謳歌・聖痕・天罰、右鍵清除の別名を追加し、複数の前提スキル名と分秒の混在する回復カウントダウンを処理する。全IDの同一英語原文の訳語を照合し、文脈により異なるReport/Tipsを除いて確実な不一致を統一した。

2.7.24をビルドして7ファイルをバックアップ後に適用。公式ランチャー2.3.10から起動し、実プロセスの`Loading [RO3 Localization Table Patcher 2.7.24]`と`[Regression] passed=550/550`を確認した。配備検証はペイロード6件一致、生成キャッシュは追記のみ、保護対象6件不変、公式更新記録3組一致、失敗0件。実画面ではクエスト見出しと戦闘中の「ジュデックス!!」表示を確認した。残る対象画面の目視確認は進行中。

今回のローカル検査: 表示47/47、履歴84,109/84,109、Recovery6/6、インストーラ12/12、辞書`--check`成功。参照先Unityアセンブリに関する既存CS1684警告は残る。配備バックアップは`../_TranslationWorkspace/deploy-backups/display-2.7.24-086908b2451144179d03d526e1c85d6b`。

## 2.7.8適用・再起動確認（19:22追記）

利用者から再起動の明示依頼を受け、通常終了後にバックアップを取り、DLL・4辞書・IDマップを実際のClientへ適用。
全6ファイルのコピー元とのSHA256一致を確認。個別設定は保持し、Recoveryの4ファイルも元バックアップとの一致を確認した。
公式ランチャーから起動し、ログで`Loading [RO3 Localization Table Patcher 2.7.8]`、`Regression passed=152/152`を確認。
ログインフォーム表示・プロセス応答を確認した。認証操作は行っていない。ゲーム内全画面の目視検証・GitHub公開は未実施。
以下の「未適用」は、その時点の過去記録。

## 8枚の追加スクリーンショット対応（2.7.8・未適用）

利用者の禁止事項に従い、ゲーム再起動・サブエージェント・実行中DLLの置換は行わなかった。
ゲームは同じプロセス（18:40起動の2.7.7）のまま。2.7.8はリポジトリ側にビルド済みであり、次の起動への配備もまだ行っていない。

|画像|原因と修正|
|---|---|
|1 ペット|中国語スキル名の別名不足。説明の数値展開と、汎用所有格訳による`SerpentのProtection`化を既知スキルテンプレートで処理。下線・色タグも検査|
|2 貿易|`价格排序`/`價格排序`の表示別名を「価格順」で追加|
|3 座標リンク|投稿者名＋地名＋座標の合成文字列。登録済み地名だけを訳し、名前・座標・未知地名・自由文は保持。実際のプレイヤー名はテストに保存しない|
|4 競技メダル|10600100004の展開済みカウンターと`WeekのCompetition`の混在形を対応。異なる数値・色タグを検査|
|5 精錬|10960000042の複数行説明と19014の安全精錬数値をテンプレート化|
|6 製作|装備種別の連結、`JobRestriction`・`KnightClass`等の空白なし表記、読点区切りの職業リストを処理|
|7 装備説明|`1H - Wand`等の種別、必要Lv＋職業制限の複数行表示、職業名途中の改行を処理。未登録職業名は元のまま保持|
|8 地図|100800/106801の登録済み地名テンプレートを描画翻訳へ追加し、階数が埋まったSphinx/Culvert表記に対応|

既存辞書の日本語を再利用し、画面ごとの全文ハードコードや任意チャットへの部分置換は避けた。
初回の画像再現検査で17件の失敗を確認してから修正した。
最終検査は履歴152件（画像対応・境界条件24件追加）と既存45件が成功。
辞書デコードと描画用ルール検査を含むhistoryランナーの総チェック数は41,263。
ビルドは成功（既存の参照DLLに起因するCS1684警告あり）。実画面の修正後確認は未実施。

XUnityのインストール済みDLLでは`OnDestroy`が`TextCache.Dispose`を呼ぶ。
したがって、起動時にコンポーネントが破棄される現状で辞書のファイル監視だけに即時反映を期待できない。
この作業では起動中のファイル変更・プロセスへの注入・再起動による制約回避はしていない。
以下は以前の版の検証記録。

## 実機への適用と検証（18:40追記）

前回の修正版はリポジトリだけにあり、ゲーム側のDLLのSHA256とは異なっていた。
利用者の再起動後も以前の2.7.6試作版で97/115のログだったため、未適用を確定した。

利用者がゲームを通常終了したことを確認し、識別用バージョンを2.7.7に更新。
DLL・4辞書・IDマップをバックアップ後に実際のClientへコピーし、全6ファイルのSHA256一致を検証した。
個別のAutoTranslatorConfig.iniは保持。公式Recoveryの4ファイルは元バックアップと一致した。

利用者が公式ランチャーから起動後、最新ログで以下を確認した。

- `Loading [RO3 Localization Table Patcher 2.7.7]`
- `[LocalizationTablePatcher][Regression] passed=128/128`

今回の128件には実際のLanguageMain経由のID別9ケースを含む。以前失敗した18件も成功した。
これは起動時の実プロセス内の合成入力検査であり、全画面の目視検証ではない。
この時点でGitHubへのpush・リリースは行っていない。以下は調査・オフライン修正時点の記録。

## 再起動なしの修正結果（同日追記）

以下の初回調査を受け、リポジトリ内のみ修正。実行中ゲームへの適用・再起動・公開は未実施。

- 辞書の`=`、バックスラッシュ、改行、コメント記号、`%3D`をXUnityの読込契約に合わせて保存。再生成後の読込不適合は0行。
- 過去の既知IDを描画用ルールの生成対象にも追加（4,792→4,821ルール）。過去に不足した16文章について、デコード後の正規表現が期待する完成形を生成することを確認。
- 描画翻訳では既知のテンプレートを汎用XUnityルールより優先。ソウルエコー・料理・ギルド・アイテム説明・募集・報酬・サーバー告知の誤一致を防止。
- 拾得アイテムと前提スキルの埋込名は、それぞれのID範囲の既知名だけを解決。未知名は保持。
- `Report`と`Tips`のID別訳をグローバルな英語一致辞書から除外。永続IDフックとローカル辞書参照は2.7.6試作版の変更を維持。
- 履歴検査を128ケースへ拡張（ID別9ケースを含む）。値は11/22/33…で区別。文字列ケースは再翻訳しても変化しないことを確認。
- 辞書41,067行の読込、過去の描画用ルール16件、履歴128件の計41,211チェックが成功。既存描画27件・インストーラー12件・Recovery6件も成功。
- `test-history.py`をリリースCIへ追加。生成済みfixtureの一致も検査する。

**検証の限界:** ゲーム外テストはXUnityのデコーダーをILから再現した.NET検査と実際のDisplayTextTranslatorを使う。
ID別9件は配布マップの値の検査であり、実ゲームのHarmonyフックを実行した結果ではない。
XUnityのライブキャッシュ・フック順序・全画面の表示・負荷は未確認。現行ゲームは以前のDLLを読み込んだまま。
以下は修正前の証拠を保持した記録。

## 結論と検証範囲

Recoveryの署名対象ファイルを変更しない方針は維持する必要がある。
過去の修正は辞書から消えたのではなく、元データの翻訳を前提にした対応の一部が描画時翻訳へ移植されていない。
現在の実行環境とリポジトリのRuntime/ Priority辞書のSHA256は一致した。

今回の調査ではゲームの再起動、UI操作、実行環境へのコピー、プロセスへの注入を行っていない。
既存の2.7.6試作版ログ（97/115成功）と、配布辞書に対するゲーム外の.NET正規表現検査を使用した。
これは実画面の全項目検証でも、修正完了の報告でもない。

## 既存ログの18件を分類

|分類|件数|証拠・対象|
|---|---:|---|
|展開済み文章に対応するルール不足|10|ソウルエコー10960000082/83、オリデオコン12390100342、料理10050100004、ギルド25172–25175/50058、募集カード34076|
|汎用所有格ルールへの誤一致|4|25163/25164/25176/50059。`Last Week's Activity: 7`が`Last WeekのActivity: 7`になる|
|色タグを含むルールが読込形式に不適合|1|40310。正規表現単体は一致するが、未エスケープの`=`で辞書行が無効になる|
|汎用ルールが複数行・タグ境界を巻き込む|1|35031。日時用キャプチャが先頭行を含み、末尾の`</color>`より後ろに「に上昇」が付く候補が生成される|
|組立後の名前の翻訳不足|2|`Milk X 7`の組立済み形式が未対応。前提スキルは「前提スキル」だけ訳され、キャプチャした`Heal`が残る|

上記はルール集合を独立評価した結果。XUnityのキャッシュ・優先順位・実画面の経路を完全に再現したものではない。
ただし、全18件に期待する完成形を生成する有効なルールがないことを、この入力集合で確認できた。

## 辞書読込形式の問題

インストール済みXUnity DLLの`TextHelper.ReadTranslationLineAndDecode`のILを確認した。
未エスケープの`=`をフィールド区切りとして扱い、余分な区切りがある行を返さない。
`\=`、`\u003D`、`%3D`は文字として扱われる。バックスラッシュと改行にもデコード段階がある。

この挙動を再現する独立検査では以下の行数が無効になった（重複を除いた画面項目数ではない）。

- RuntimePlaceholders: 4,792行中16行。
- CanonicalTranslations: 26,614行中286行。
- PriorityOverrides: 9,631行中0行。

生成処理は現在`key=value`をそのまま書き出しており、辞書フィールドのエスケープ検証がない。
Recovery停止がこの形式問題を作ったわけではないが、元データ側での翻訳がなくなり、隠れていた不足が現れる。

## 過去の対応との照合

|コミット|対応|Recovery停止後に必要な処置|
|---|---|---|
|642f974 / 4219609|前提スキル・装飾付きツールチップ|テンプレートに加え、既知の埋込スキル名を翻訳する。タグ・改行の読込後の一致を検査する|
|506cfb8 / 754765b / ffa36a6 / df67d38|アイテム説明・拾得名・活力とカード能力・料理と地名|元データ差替えに依存した範囲を描画用辞書へ移す。動的IDの少数ホワイトリストだけでは不足|
|55a11f4 / 7f0af7d|ギルド目標・活動表示|展開済みの数値付き文章を対象に追加し、汎用所有格ルールの誤一致を防ぐ|
|ea465ef / 700dc26 / 9e83b88|分割カプラ通知・PTとボス表示・戦績と告知|分割表示を保護し、同じ英語でも異なる意味のボタンをIDで区別する|
|098725c / a20f4ca / 123975c|MVPカウンタ・活動カード・更新後の動的UI|既存の27件テストに加え、他の過去対応範囲も展開後入力で検証する|
|8d3ac29 / dd1e27b|繁体字露店・カレンダー・孵化|既存別名を維持。孵化の色タグ付きルールの読込を修正する|
|4cb8ebf|使用回数ツールチップ・キャラバン支援|回数付き表示に加え、`Tips`のID25368を検査する|
|b64e652 / 31eeac8 / 2588e55|Recovery書込停止・描画翻訳・寿命修正|署名対象は変更しない。Unityコンポーネント破棄後も必要なフックを維持する|

## 検査側の抜け

`build-history-checks.py`は`KNOWN_LOCALIZATION_PATCH_IDS`だけを列挙している。
`id:`用の分岐はあるが、対象の戦績・支援IDがその集合にないため、現在の115件にはID別テストが一件もない。
`KNOWN_LOCALIZATION_ID_JAPANESE_OVERRIDES`を別途列挙する必要がある。
また全プレースホルダーを同じ`7`にしており、順序交換を検出できない。数値・日時・既知名・未知名・複数行で異なる値が必要。

## 次の修正順序

1. 辞書のフィールド保存・読込契約を修正し、実際のデコード後の完全一致を回帰検査する。
2. 元データ差替え対象と描画時ルールの対象を照合し、確認済みの不足範囲を補う。
3. 特定テンプレートを汎用ルールより優先し、改行・色タグをキャプチャに巻き込ませない。
4. 埋込名は既知のアイテム・スキル辞書でのみ解決する。プレイヤー名・自由文は一律置換しない。
5. ID別の意味、異なる値、二度翻訳しても壊れないことをゲーム外で検査する。
6. 実機反映と表示確認は別工程。現時点では未実施・未push。

再現用の読み取り専用診断:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\inspect-history-rules.ps1 -Details
```

診断スクリプトは生成済み115件のうち既存ログの失敗18件を調べる。ゲームに接続せず、辞書・設定を変更しない。

## 2.7.9 追加スクリーンショット対応

- 価格昇降順、編成、能力値、地域、中国語UIラベルを補完。
- タレント・ボス説明・ペット説明の既知テンプレートを描画フォールバックに追加。所有格が「の」に変換済みの入力も処理する。
- 購入制限、購入資格、塔の階層条件、ビルド番号は同系列IDを対象化。既知アイテム名の小文字x/×数量、全曜日の時刻連結、改行された推奨人数にも対応。
- 能力値の後続部分は数値・符号・小数・%だけ許可。短い単位テンプレートが一般英文の末尾s/dを誤変換する回帰を修正。
- オフライン表示ケース240件、辞書契約を含む41888チェック、既存表示27件、インストーラ12件、Recovery6件が成功。ビルド成功（既存CS1684警告あり）。
- 実機6ファイルと240件の開発用fixtureをバックアップ付きで適用し、SHA256一致を確認。個人設定と署名済みRecoveryは変更しない。
- バックアップ: `../_TranslationWorkspace/deploy-backups/history-2.7.9-8943045f00864d109e838b17acd9d076`。
- 画像埋め込みの可能性があるイベント見出しと、ログイン後の全画面の目視確認は未完了。辞書テスト成功はそれらの画面確認を意味しない。未push。
- 正式ランチャーから再起動し、2.7.9ロードと実プロセス内の`[Regression] passed=240/240`を確認。ログインフォーム表示まで確認済み。認証操作はComputer Useの禁止事項に従い利用者へ引き継ぐ。

## 2.7.10 ログイン後の目視確認と追加修正

- 2.7.9実画面で精霊の塔ショップの「ペットの卵 x5」「シーズン購入上限」「精霊の塔140階をクリア」「売り切れ」、ギルドショップの週間購入上限を確認。購入操作なし。
- ギルド役職名が文中で英語のままになることと、スキル画面のBuild 1が「1をビルドする」になる誤訳を発見。役職9種類をID対象に加え、完全一致の購入条件文を生成。描画フォールバックの動的ルールより優先する。ビルド名は「ビルド 1」へ元辞書を修正。
- 役職9種類×英語/混在2形式とビルド名を追加した268表示ケース、全41943チェック、既存27表示ケースが成功。ビルド成功（既存CS1684警告）。
- 6ファイルをバックアップ付きで適用し、全SHA256一致を確認。バックアップ: `../_TranslationWorkspace/deploy-backups/history-2.7.10-9418417deee84d899736457c8e7dac41`。
- 追加2点のログイン後再目視と、残りのイベント・能力値・ボス説明画面は未確認。未push。
- 2.7.10を正式ランチャーで起動。ログの版数と実プロセス内`[Regression] passed=268/268`を確認。
- 利用者の再ログイン後、2.7.10のスキル選択欄「ビルド 1」とギルドショップ購入条件の「副ギルドマスター」を実画面で確認。役職の英語残りは解消。ただし購入条件末尾はカード幅でクリップされるため、全文の視認性は未解決。購入・編成変更は行わず、確認画面を閉じてゲームへ戻した。

## 2.7.16〜2.7.18 履歴・作業ツリー・追加画像の再監査

### 原因と修正範囲

`b64e652`、`31eeac8`、`2588e55`、`123975c` と、それ以前の翻訳漏れ修正コミットを照合した。正規辞書が一括削除されたのではなく、署名付きRecoveryのLuaを直接変更する経路から描画時の翻訳へ移行した際に、補間後の表示や初期値を持つUI部品を十分に拾えていなかった。既存の未コミット差分も含めて検査し、元の作業変更を保持した。

旧ID対応範囲に相当する17,318件の上書きと、26,615件の正規辞書を描画処理へ読み込む。既知テンプレート全体を使って数値や色指定を保持し、固定文は汎用テンプレートより優先する。TMP、Unity UI、HUD/MeshUIの文字設定経路に加え、setterを通らずprefabの初期テキストで表示する部品も有効化時に補完する。

最初の履歴検査では81,986チェック中31件が失敗した。装飾や文脈を除去した派生キーが、`Captain`、`Fixed CT`、`Body Tempering` など既存の正規キーを異なる訳で上書きしていた。派生キー生成時に正規訳を保護し、描画の完全一致検索でも正規辞書を優先した。`[Event]` のように装飾除去で異なるIDが同じ原文になる場合は、ID固有の訳をID呼び出しで検査し、正規訳は別の完全一致検査で確認する。

追加画像への対応は次の通り。

| 対象 | 対応 |
| --- | --- |
| 蛇霊の加護の追加効果 | buff説明ID群と、所有格が「の」になった補間後の英文にも対応 |
| クエスト | 既知のクエスト名と章名を組み合わせ、色タグ・区切り・数値を保持 |
| カードコレクション | 収集数の補間に対応。簡体字・繁体字の固定prefab見出しを追加 |
| イベント案内 | 生きた石炭の採集回数を含むイベント説明ID群を追加 |
| 製作・装備ツールチップ | 半角／全角コロン、改行されたジョブ名、MageClass等の連結表記を処理 |
| 共鳴・マップ・戦闘・獲得通知 | 過去のID対応、地名別名、魔物名、色付き数量、共鳴の現在値を検査対象に保持 |

### 実画面からの追加修正

2.7.16を配備してクエスト画面を開き直すと、見出しは日本語化された一方、本文が英語で残った。実ログでは、固定の説明文とサーバーレベル上限2行が単一TMPテキストへ連結されていた。2.7.17で既知の導入文を分離して後続行も翻訳し、再起動後の同画面で本文・上限・更新日時を確認した。オークション画面で見つけた `20:45 Ends` は、時刻形式だけに限定して終了表示へ変換した。

2.7.17の実ログでは、地図リンクの後ろに利用者の自由文があるチャット、宝探しの出現通知、`Eddga has spawned on the 斐扬树林 map.` に地名が残っていた。2.7.18では数値形式の `findpath` リンク内の既知地名だけを変換し、周辺の自由文・送信者名・リンク先・座標は保持する。出現通知は既知の魔物名と地名を処理する。通常のリンクや不明な経路を変換しない負例も加えた。

### 確認結果と再実行

- 2.7.18の履歴・辞書・描画検査: **82,179 / 82,179成功**。生成fixtureは7,660件で、既存の完全一致・辞書デコード検査も含む。
- 既存表示27件、インストーラ12件、Recovery6件が成功。DLLビルド成功。参照先Unityアセンブリに関する既存CS1684警告は残る。
- 2.7.16実プロセス413件、2.7.17は419件、2.7.18は427件が成功。これはゲームプロセス内で同じ翻訳器・ID呼び出しを使う検査であり、427種類の画面を目視したという意味ではない。
- 実画面では章付きクエスト見出し・連結本文、カード収集数、中国語の絞り込み見出し、獲得コイン・アイテム通知を確認した。共有された座標のチャット履歴でも地名変換を実ログで確認した。
- 開催時間に依存するイベント案内は、その実場面を再現できていない。補間後の原文・色タグ・回数のパターンは履歴検査と実プロセス内検査で確認した。

```powershell
py -X utf8 _TranslationWorkspace\import_translations.py --check
py -X utf8 scripts\test-history.py
py -X utf8 scripts\test-display.py
py -X utf8 scripts\test-installer-paths.py
py -X utf8 scripts\test-recovery.py
```

履歴fixtureは `scripts/build-history-checks.py` で再生成する。`test-history.py` は生成内容の一致を確認してから、PowerShell/C#側で実際の翻訳器を検査する。実ログ由来のfixtureは利用者名・IDをダミーに置き換えてある。

### 配備と調査中の公式更新

各配備でDLL・ID上書き・別名・4辞書の7ファイルをバックアップ後に適用し、SHA-256一致を確認した。起動後の `_AutoGeneratedTranslations.txt` はキャッシュ追記が発生するため、配備済み内容が完全に保持された追記だけを許容する。個人設定、英語・簡体字・繁体字のLocalization Luaは変更していない。

調査中に公式ランチャーが2.3.8から2.3.9へ更新を要求し、続くUpdate Gameで24ファイルを適用した。ランチャーのログには2026-09-23 13:26:01 UTCの `baseline_rolled_forward` / `apply_committed`、13:27:00 UTCの `launcher_post_launch_health_accepted` が記録された。manifestの差分はLocalization以外の21モジュールで、3言語Luaのハッシュは更新前と一致する。manifest・署名の更新を翻訳パッチによる変更と混同しないよう、前後ハッシュを手動確認済みの別記録に保存した。これは公式ランチャーでの適用観察であり、別実装による暗号署名検証を行ったという意味ではない。

`verify-display-deployment.py` は標準では公式ファイルの差分も失敗として報告する。明示的な `--official-update` 記録を渡した場合だけ、元の配備と連続する固定ハッシュのmanifest・署名を認める。それ以外の保護ファイルの差分、旧バックアップの欠損・改変、新しい未記録ハッシュは失敗となる。旧manifestを公式更新後へ強制復元していない。

最終配備バックアップ: `../_TranslationWorkspace/deploy-backups/display-2.7.18-8bfd7261855142658ac1536258567d2e`。
公式更新記録: `../_TranslationWorkspace/official-update-20260923-132601.json`。

```powershell
py -X utf8 scripts\verify-display-deployment.py --backup ..\_TranslationWorkspace\deploy-backups\display-2.7.18-8bfd7261855142658ac1536258567d2e --version 2.7.18 --official-update ..\_TranslationWorkspace\official-update-20260923-132601.json
```

この作業ではコミット・push・Release公開を行っていない。
